import { test } from "@playwright/test";

/** Keeps a placeholder Playwright suite in place until real end-to-end flows exist. */
test("app shell placeholder", async () => {
  test.skip(true, "Phase 0 only scaffolds the app shell and route structure.");
});
